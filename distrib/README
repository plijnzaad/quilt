I'll sort this out later:

  lijnzaad-quilt.tar.gz
  lijnzaad-quilt.ls
  ftp-quilt.tar.gz
  ftp-quilt.ls
  quilt-0.9.tar.gz
  quilt-0.9.ls
  lijnzaad-src.tar.gz
  lijnzaad-src.ls

for now, everything is unwriteable, and backed up to ~cvs/backup
